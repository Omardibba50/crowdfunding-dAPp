export function trackedList(config) {
    const { name, metrics } = config;
    const list = [];
    metrics?.registerMetric(name, {
        calculate: () => {
            return list.length;
        }
    });
    return list;
}
//# sourceMappingURL=tracked-list.js.map